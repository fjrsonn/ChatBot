Chatbot0.2/
├── dashboard/
│   ├── app/
│   │   ├── __init__.py    ← Arquivo necessário para tornar 'app' um pacote
│   │   ├── main.py        ← Arquivo principal que executa o app Flask
│   │   ├── models.py      ← Define os modelos de banco de dados
│   │   ├── routes.py      ← Define as rotas (como /admin)
│   │   └── templates/
│   │       └── dashboard.html 
├── Dockerfile             ← Se estiver usando Docker
└── ...

